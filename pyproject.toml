[project]
name = "mongodbassignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Siddharth-Suddala", email = "siddharth.s2404@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "pymongo>=4.18.1",
]

[project.scripts]
mongodbassignment = "mongodbassignment:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
